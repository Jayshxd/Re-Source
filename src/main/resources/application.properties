spring.application.name=Re-Source
# Agar Railway pe hai to waha ke variable lega, nahi to localhost
spring.datasource.url=${MYSQL_URL}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
# Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=${PORT}

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect


jwt.secrete.key=ThisIsAVeryVeryVeryImpSecretKeyHereIs

# SMTP server host (e.g., smtp.gmail.com, smtp.mailtrap.io)
spring.mail.host=smtp.gmail.com

# SMTP server port (e.g., 587 for TLS, 465 for SSL)
spring.mail.port=587

# Username for authentication with the SMTP server
spring.mail.username=gareebadmi2504@gmail.com

# Password for authentication with the SMTP server (consider using app passwords for services like Gmail)
spring.mail.password=tbzh hpzr whhq cssn

# Enable SMTP authentication
spring.mail.properties.mail.smtp.auth=true

# Enable STARTTLS for a secure connection (recommended for most modern SMTP servers)
spring.mail.properties.mail.smtp.starttls.enable=true
